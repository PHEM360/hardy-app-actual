# FCM Web Push VAPID public key (Firebase Console → Project Settings → Cloud
# Messaging → Web Push certificates). This is a public key, safe to commit —
# it's the same key Vite bakes into the client bundle either way.
VITE_FCM_VAPID_KEY=BBvH0_GVjpnD8UoklRRfuD_dJb-ObjBgqu0WdYRqH3qm7vCIqV1fOoJDGIN3XAi-A5Xxwc05EIxgaPf6JuwyTtA

# Canonical public base URL — used to build QR codes and printed/shared links
# so they encode the real domain instead of localhost. See src/lib/appUrl.ts.
VITE_APP_BASE_URL=https://hardyapp.co.uk
